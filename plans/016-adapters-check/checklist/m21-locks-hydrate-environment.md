# M21 — Vitest: environment hydrates

**Track:** Merchant · **Depends:** M11  
**Goal:** Grep `setEnvironment` used with GET body.

---

## M21.1

- [ ] `WorkspacePage.tsx` contains `setEnvironment` from `body.environment` (or equivalent)
- [ ] Lock greps `environment` assignment from gateway GET, not only the select default `'test'`

## M21.2 Exit

- [ ] Green
