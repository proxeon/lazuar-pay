import { notFound } from "next/navigation";

// Hidden: Community module — route disabled. Restore from git history to re-enable.
export default async function CommunityPortalPage() {
  notFound();
}
